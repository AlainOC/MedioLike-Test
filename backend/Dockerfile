FROM node:20-alpine

WORKDIR /app

# Esto es un placeholder. Cuando el Backend (Pablo) inicialice el proyecto:
# 1. Copia package.json y ejecuta npm install
# 2. Reemplaza el comando por el servidor real

CMD ["sh", "-c", "echo 'Backend Express Container Inicializado. Pendiente de implementación por Pablo.' && tail -f /dev/null"]
